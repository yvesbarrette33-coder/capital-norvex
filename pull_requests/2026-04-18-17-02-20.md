# Pull Request Title
fix: correct X-Internal-Secret header verification in Netlify functions

# Pull Request Body
These 4 functions now correctly verify the X-Internal-Secret header in lowercase format and compare against process.env.INTERNAL_SECRET. Requires INTERNAL_SECRET env var to be set in Netlify Site Settings.